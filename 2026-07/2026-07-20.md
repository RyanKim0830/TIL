# TIL
모델 설계 (DB 테이블 구조 설계)를 하고 있는데, 필드라는 용어가 나옴
필드: 데이터에 대한 속성? 즉 객체에 대한 속성이라고 보는듯, ex) 사람 -> 필드: 이름, 키, 나이 등

또 오랜만에 파이썬 기본 문법 복습함
> lambda -> '함수 실행'을 넘기기위함이다, 즉, 결괏값이 아니라는 것

UUID 로 고윳값을 뽑을라고 쓰고있고,

Python에서 class()에서 ()안에 들어가면 상속받을 부모 객체 ㅇㅇ

ORM을 왜쓰냐 -> SQL 을 쓰려면 문자열로 쓰기때문에 코드가 좆같아지고 SQL 구문 오류도 못잡음 
따라서 ORM으로 간편하게 DB 제어,

index/unqiue -> ORM 옵션이였는데, index는 조회를 빠르기 하기 위함이고, unique는 중복되지 않아야함을 의미 한다고 함.
